import { createDesignSystemConfig } from '../config/createDesignSystemConfig';

// Consumers can import this as a reference for UISmith defaults.
export const exampleDefaultConfig = createDesignSystemConfig();

